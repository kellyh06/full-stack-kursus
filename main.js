let sum = (p1, p2) => {
    console.log(p1)
    console.log(p2)
    return p1 + p2
}
let result = sum(1, 5)
console.log(result)

let sq = p => {
    console.log(p)
    return p * p
}
let square = p => p * p
let t = [1, 2, 3]
let tSquared = t.map(p => p * p)

function product(a, b) {
    return a * b
}

let re = product(2, 6)

let average = function(a, b) {
    return (a +b) / 2
}
let resul = average(2,5)
